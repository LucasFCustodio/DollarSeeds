[pytest]
testpaths = tests
# Negative tests deliberately sign tokens with short "attacker guessed this" secrets;
# PyJWT's key-length advisory is expected noise there, not a finding.
filterwarnings =
    ignore::jwt.warnings.InsecureKeyLengthWarning
